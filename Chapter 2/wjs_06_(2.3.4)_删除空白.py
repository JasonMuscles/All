import this

# 通过rstrip()删除空白
name = " WangJason "

# rstrip 去除末尾空白
name_rstrip = name.rstrip()
print(name_rstrip)

# lstrip 去除末尾空白
name_lstrip = name.lstrip()
print(name_lstrip)

# strip 去除开头、末尾空白
name_strip = name.strip()
print(name_strip)